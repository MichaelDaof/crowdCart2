var chai = require('chai');

var assert = chai.assert;
var should = chai.should();
var expect = chai.expect;

describe('the crowdCart2 user interface', function() {

  //put all your tests related to the front end in here! (stuff in the client folder)

});



describe('the crowdCart2 server', function() {

  //put all your tests related to the back end in here!(stuff in the server folder)

});


// as a reminder, you'll need to run npm install before you can do npm test